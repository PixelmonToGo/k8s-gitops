---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: minecraft-forge-charmander-dev
  namespace: minecraft-dev
spec:
  selector:
    matchLabels:
      app: minecraft-forge-charmander-dev-prometheus
  namespaceSelector:
    matchNames:
      - minecraft-dev
  endpoints:
    - port: prometheus
      interval: 15s
      scrapeTimeout: 10s
      honorLabels: true
